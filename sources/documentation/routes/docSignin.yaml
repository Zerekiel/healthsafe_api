paths:
#GET
  /api/signin/signinId:
    get:
      tags:
        - SIGNIN
      summary: /api/signin/signinId
      description: By ID, display an user connected.
      consumes:
        - "application/json"
      produces:
        - "application/json"
      requestBody:
        description: Need ID.
        type: object
        content:
          application/json:
            schema:
              $ref: '#/definitions/idDefRequest'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/definitions/signinModelRes'
        204:
          description: OK -> No content in DB.
          content:
            application/json:
              schema:
                $ref: '#/definitions/204DefResSuccess'
        500:
          description: ERROR -> check error stack display for more information.
          content:
            application/json:
              schema:
                $ref: '#/definitions/500DefResError'

  /api/signin:
    get:
      tags:
        - SIGNIN
      summary: /api/signin
      description: Display all user connected. Returns a JSON list of all user connected.
      consumes:
        - "application/json"
      produces:
        - "application/json"
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/definitions/signinModelRes'
        204:
          description: OK -> No content in DB.
          content:
            application/json:
              schema:
                $ref: '#/definitions/204DefResSuccess'
        500:
          description: ERROR -> check error stack display for more information.
          content:
            application/json:
              schema:
                $ref: '#/definitions/500DefResError'

# POST
    post:
      tags:
        - SIGNIN
      summary: /api/signin
      description: Allows the user to sign in his credentials and generate a bearer token. Returns JSON
      consumes:
        - "application/json"
      produces:
        - "application/json"
      requestBody:
        description: Email and password are required.
        type: object
        content:
          application/json:
            schema:
              $ref: '#/definitions/signinModelReqPOST'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/definitions/signinModelRes'
        400:
          description: ERROR BAD REQUEST
          content:
            application/json:
              schema:
                $ref: '#/definitions/400signinDefResError'
        500:
          description: ERROR -> check error stack display for more information.
          content:
            application/json:
              schema:
                $ref: '#/definitions/500DefResError'

  /api/signin/create:
    post:
      tags:
        - SIGNIN
      summary: /api/signin/create
      description: Add user into signin collection and generate a bearer token. Returns JSON
      consumes:
        - "application/json"
      produces:
        - "application/json"
      requestBody:
        description: Email and password are required.
        type: object
        content:
          application/json:
            schema:
              $ref: '#/definitions/signinModelReqPOST'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/definitions/signinModelRes'
        # 400:
        #   description: ERROR BAD REQUEST
        #   content:
        #     application/json:
        #       schema:
        #         $ref: '#/definitions/400signinDefResError'
        500:
          description: ERROR -> check error stack display for more information.
          content:
            application/json:
              schema:
                $ref: '#/definitions/500DefResError'

# DELETE
  /api/signin/delete:
    delete:
      tags:
        - SIGNIN
      summary: /api/signin/delete
      description: Delete user in DB by ID. Returns JSON.
      requestBody:
        description: Need ID. Other keys are not mandatory.
        type: object
        content:
          application/json:
            schema:
              $ref: '#/definitions/idDefRequest'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/definitions/signinModelRes'
        204:
          description: OK -> No content in DB.
          content:
            application/json:
              schema:
                $ref: '#/definitions/204DefResSuccess'
        500:
          description: ERROR -> check error stack display for more information.
          content:
            application/json:
              schema:
                $ref: '#/definitions/500DefResError'

# UPDATE
  /api/signin/update:
    put:
      tags:
        - SIGNIN
      summary: /api/signin/update
      description: Update user's data in DB by ID. Returns JSON with new data.
      requestBody:
        content:
          application/json:
            schema:
              type: object
              $ref: '#/definitions/signinModelReqPUT'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/definitions/signinModelRes'
        500:
          description: ERROR -> check error stack display for more information.
          content:
            application/json:
              schema:
                $ref: '#/definitions/500DefResError'
