components:
  schemas:
    signupProfilePatientComponent:
      tags:
        - MODELS
      type: "object"
      properties:
        signupProfilePatientModel:
          - $ref: '#/definitions/signupProfilePatientModel'
