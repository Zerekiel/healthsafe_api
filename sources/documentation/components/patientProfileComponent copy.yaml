components:
  schemas:
    patientProfileComponent:
      tags:
        - MODELS
      type: "object"
      properties:
        patientProfileModel:
          - $ref: '#/definitions/patientProfileModel'
