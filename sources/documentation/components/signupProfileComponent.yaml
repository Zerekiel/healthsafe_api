components:
  schemas:
    signupProfileComponent:
      tags:
        - MODELS
      type: "object"
      properties:
        signupProfileModel:
          - $ref: '#/definitions/signupProfileModel'
