definitions:
  400drSigninDefResError:
    type: object
    properties:
      errors:
        type: string
        description: Bad Request.
        example : 'Login failed! Check authentication credentials'
